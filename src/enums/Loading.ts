export enum LoadingType {
    SPINNER = 'spinner',
    DYNAMIC_SPINNER = 'dynamic_spinner',
    DOTS = 'dots',
}

export enum LoadingColor {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    WHITE = 'white',
    BLACK = 'black',
}